# 부동산 쇼츠 스튜디오 V3.1

## 이번 수정
- 사진 업로드 후 미리보기가 멈추던 캔버스 ID 오류 수정
- 사진 업로드 즉시 `장면 1 / N` 표시
- 이전/다음 장면 정상 동작
- 실시간 9:16 미리보기 정상화
- 검정 자막 박스 없이 POP 자막 표시
- `작게 → 118% → 100%` 빠른 POP 모션
- TTS boundary 이벤트가 있는 브라우저에서는 실제 발화 위치로 자막 동기화
- boundary 이벤트가 없는 환경에서는 경과시간 기반 fallback으로 자막이 멈추지 않도록 처리
- 템플릿 / 강조색 / BGM / 효과음 기능 유지

## 실행
Chrome에서 `index.html`을 열거나 정적 웹 호스팅에 배포합니다.
